# Use o while loop para gerar um numero randomico entre 1 e 10
# Guarde o valor gerado na variavel numero
# O loop deve executar até que o número gerado seja 5
# Toda vez que o loop executar incremente 1 a variavel i
# Imprima o valor de i a cada iteração

####  NÃO ALTERAR ####
from random import randint # use randint(a,b) para gerar um um numero randômico entre a e b
####  NÃO ALTERAR ####

numero = 0 
i = 0

